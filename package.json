{
  "name": "node-template",
  "version": "0.0.1",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "test": "jest",
    "build": "tsc",
    "prestart": "npm run build"
  },
  "devDependencies": {
    "@types/jest": "25.1.4",
    "jest": "25.1.0",
    "ts-jest": "25.2.1",
    "typescript": "3.8.3"
  },
  "jest": {
      "preset": "ts-jest",
      "testEnvironment": "node",
      "collectCoverage": true
  }
}
